#!/usr/bin/env just --justfile

_default:
    @{{ just_executable() }} -f {{ justfile() }} --list

# build all images
build-all:
    fedora/fedora.sh --all

# publish all images
publish-all:
    fedora/fedora.sh --publish --all
