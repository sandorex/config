# Customized dark theme for dialog (made using nightfox/carbonfox theme)
#
# Automatically generated by "dialog --create-rc <file>"
#
# Types of values:
#
# Number     -  <number>
# String     -  "string"
# Boolean    -  <ON|OFF>
# Attribute  -  (foreground,background,highlight?,underline?,reverse?)
# 0 black, 1 red, 2 green, 3 yellow, 4 blue, 5 magenta, 6 cyan and 7 white

# Set aspect-ration.
aspect = 0

# Set separator (for multiple widgets output).
separate_widget = ""

# Set tab-length (for textbox tab-conversion).
tab_len = 0

# Make tab-traversal for checklist, etc., include the list.
visit_items = OFF

# Shadow dialog boxes? This also turns on color.
use_shadow = ON

# Turn color support ON or OFF
use_colors = ON

# Screen color
screen_color = (BLACK,BLACK,OFF)

# Shadow color FOREGROUND MEANS NONE
shadow_color = (BLACK,BLACK,ON)

# Dialog box TEXT color
dialog_color = (WHITE,BLACK,OFF)

# Dialog box title color
title_color = (WHITE,BLACK,ON)

# Dialog box border color
border_color = (MAGENTA,BLACK,ON)

# Active button color
button_active_color = (WHITE,BLACK,ON,OFF,ON)

# Inactive button color
button_inactive_color = (WHITE,BLACK,OFF)

# Active button key color
button_key_active_color = (WHITE,BLACK,ON)

# Inactive button key color
button_key_inactive_color = (MAGENTA,BLACK,ON)

# selected button label color
button_label_active_color = (WHITE,BLACK,ON,OFF,ON)

# Inactive button label color
button_label_inactive_color = (BLACK,BLACK,ON)

# Input box color
inputbox_color = (WHITE,BLACK,OFF)

# Input box border color
inputbox_border_color = (GREEN,RED,ON)

# Search box color
searchbox_color = (RED,GREEN,OFF)

# Search box title color
searchbox_title_color = (MAGENTA,GREEN,ON)

# Search box border color
searchbox_border_color = (GREEN,GREEN,ON)

# File position indicator color
position_indicator_color = (WHITE,BLACK,O)

# Menu box color
menubox_color = (WHITE,BLACK,OFF)

# Menu box border color
menubox_border_color = (MAGENTA,BLACK,OFF)

# Item color
item_color = (WHITE,BLACK,OFF)

# Selected item color
item_selected_color = (WHITE,BLACK,ON,OFF,ON)

# Tag color
tag_color = (MAGENTA,BLACK,ON)

# Selected tag color
tag_selected_color = (CYAN,BLACK,ON,OFF,ON)

# Tag key color
tag_key_color = (RED,BLACK,OFF)

# Selected tag key color
tag_key_selected_color = (RED,BLACK,ON)

# Check box color
check_color = (WHITE,BLACK,OFF)

# Selected check box color
check_selected_color = (WHITE,BLACK,ON)

# Up arrow color
uarrow_color = (GREEN,GREEN,ON)

# Down arrow color
darrow_color = (GREEN,GREEN,ON)

# Item help-text color
itemhelp_color = (GREEN,RED,OFF)

# Active form text color
form_active_text_color = (WHITE,MAGENTA,ON)

# Form text color
form_text_color = (CYAN,MAGENTA,OFF)

# Readonly form item color
form_item_readonly_color = (WHITE,MAGENTA,OFF)

# Dialog box gauge color
gauge_color = (MAGENTA,WHITE,ON)

# Dialog box border2 color
border2_color = (MAGENTA,BLACK,ON)

# Input box border2 color
inputbox_border2_color = border2_color

# Search box border2 color
searchbox_border2_color = border2_color

# Menu box border2 color
menubox_border2_color = border2_color
