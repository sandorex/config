#!/bin/bash
#
# cgit - just a wrapper to call the git on config repo, simpler than bunch
# of scripts inside shell rc

DOTFILES="$(realpath "$(dirname "${BASH_SOURCE[0]}")/../..")"

if [[ "$PWD" == "$DOTFILES" ]]; then
    git "$@"
else
    git -C "$DOTFILES" "$@"
fi

