#!/usr/bin/env bash
#
# task/edit - edits task directly

DIR="$HOME/.task"

[[ ! -f "$DIR/.current.md" ]] && exit 1

# open the file directly instead of the link
command "$EDITOR" "$DIR/$(readlink "$DIR/.current.md")"

